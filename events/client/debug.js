const { Client } = require("discord.js");

module.exports = {
    name: "debug",
    /**
     * @param {Client} client
     * @param {String} info
     */
    run: async (client, info) => {
        // console.log(`[ DEBUG ] ${info}`);
    }
}